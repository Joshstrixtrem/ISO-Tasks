#Primero se crea la carpeta para poder correr los archivos usando este comando mkdir clase-3-enrutador

#luego abrimos esa carpeta desde cmd cd clase-3-enrutador

#Luego ejecutamos con el siguiente comando:
node server.js